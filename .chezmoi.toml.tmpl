[git]
	autoCommit = true
	autoPush = false
[data.git]
	{{- if not (hasPrefix "JAMF" .chezmoi.hostname) }}
	email = {{ onepasswordRead "op://Private/GitHub/email" | quote }}
	{{- else }}
	email = {{ promptString "Git Email" | quote }}
	{{- end -}}
